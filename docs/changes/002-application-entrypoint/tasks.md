# 实施任务：应用启动入口

## 进度总览

- 总进度：`8 / 10` 点。

## 流程偏差

- `be76f39 feat: 新增应用启动入口` 在任务方案未经用户确认时完成实现与提交。
- 用户在 2026-08-11 选择保留代码并记录偏差，但未追认实现完成；`VER-001` 继续保持未完成。
- 后续对本入口的任何非文档修改必须按 `docs/changes` 确认门禁重新建立或更新任务方案。

## 任务清单

- [x] **APP-001（4 点）** 实现 `cmd/app` 的 CLI/服务双模式入口、信号 Context、错误链和退出码。
- [x] **APP-002（2 点）** 为配置生成、服务启动失败和进程退出语义补充测试。
- [x] **DOC-001（2 点）** 建立任务级设计文档，并把实际启动方式同步到根 README。
- [ ] **VER-001（2 点）** 完成格式化、构建、测试、race、vet、运行探测和 Diff 检查。

## 完成条件

- 入口只组合现有 Kernel 能力，不引入业务占位实现。
- CLI 模式不读取启动配置；服务模式不掩盖配置或 Database 失败。
- 所有验证证据写入本文件的执行记录。

## 执行记录

| 轮次 | 日期 | 完成任务 | 本轮点数 | 累计点数 | 验证 | Commit | 剩余风险 |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
| 1 | 2026-08-11 | APP-001、APP-002、DOC-001 | 8 | 8 / 10 | `gofmt -l .`、`go mod tidy -diff`、`go build ./...`、`go test ./...`、`go test -race ./...`、`go vet ./...` 通过；构建产物执行 `config init` 成功；无参数执行真实进入 Kernel 并按设计拒绝空 Database engine | `feat: 新增应用启动入口` | 本机没有 Database 环境变量、3306/5432 监听、Database 服务或容器运行时，无法完成 Database 就绪后的长期运行与信号退出验证，VER-001 保持未完成。 |
