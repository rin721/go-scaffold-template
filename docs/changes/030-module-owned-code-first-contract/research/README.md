# 030 研究档案

本目录保存 030 任务的研究记录。研究先于计划；任何结论必须能被代码、生成物、依赖或已确认决策中的证据支持，事实与推断分开标记。

## 索引

- [R001 当前 HTTP 契约方向与模块耦合复核](R001-current-contract-direction/report.md)：复核当前 spec-first 管线的 authority 归属、模块依赖、第三方暴露与既有决策证据。
- [R002 typed code-first 生成路径与工具可行性](R002-code-first-generation-paths/report.md)：比较 code-first 生成方案，并基于已固定版本与本地模块缓存核实工具可行性。
- [R003 通用 HTTP 契约能力分层与迁移边界](R003-generic-contract-capability/report.md)：设计项目自有通用 HTTP 契约能力的分层、封装边界与单轨迁移顺序。

## 记录要求

每份研究记录固定包含 `metadata.yaml` 与 `report.md`。检索从 `metadata.yaml` 的 question、keywords、scenarios 进入；证据以 `report.md` 为准。