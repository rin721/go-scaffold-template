# 实施任务：应用模块命名迁移

## 1. 当前门禁

- 研究门禁：**已通过**，证据见 [R001](research/R001-business-directory-semantics/report.md)。
- 计划状态：**已确认**。
- 实施状态：**已完成实现与本地验证**。
- 当前授权：用户在计划报告后的后续消息中明确要求“执行 `应用模块命名方案`”，授权当前 `PATH-001/SEM-001/GOV-001/DOC-001/VER-001`。

## 2. 任务依赖

```text
RES-001 -> PLAN-001 -> 用户确认
用户确认 -> PATH-001 -> SEM-001 -> GOV-001 -> DOC-001 -> VER-001
```

## 3. 任务清单

| ID | 工作量 | 任务 | 完成条件 | 状态 |
| --- | ---: | --- | --- | --- |
| `RES-001` | S | 核验目录职责、术语所有权与引用面 | 当前代码、组合根、架构门禁、012/014 研究和测试基线可复核 | 已完成（文档） |
| `PLAN-001` | S | 冻结 `module` 单轨命名方案 | 目录、包名、`module.ID`、owner ID、历史策略、非目标和验证明确 | 已完成（文档） |
| `PATH-001` | M | 迁移根目录与全部 Go imports | `internal/module/todo` 成为唯一模块路径，旧目录不存在 | 已完成（实现） |
| `SEM-001` | S | 同步包限定名、身份类型、owner ID 与诊断文本 | `module.*` 语义一致，`business.*` 当前实现无残留 | 已完成（实现） |
| `GOV-001` | S | 更新 package graph 门禁与测试 | Module core 越界仍被自动拒绝，合法图通过 | 已完成（测试） |
| `DOC-001` | M | 同步当前说明并标记历史命名 | 权威入口使用 Module；历史记录不伪装成当前路径 | 已完成（文档） |
| `VER-001` | M | 执行残留搜索与全量验证 | 结构、Go、行为、文档和 Diff 门禁全部通过 | 已完成（本地验证） |

## 4. 提交与回退

- 016 计划文档、目录迁移、测试和当前说明作为一个 Conventional Commit 提交。
- 只暂存本任务文件；不修改或纳入本地配置、数据库、构建产物或范围外内容。
- 回退该单个提交可以恢复旧命名；本任务不做数据迁移或外部写入。
- 若出现 HTTP/CLI/配置/数据库行为变化或需要新增兼容层，退回研究与计划并重新确认。

## 5. 计划阶段记录

| 轮次 | 日期 | 完成任务 | 证据 | Commit | 剩余风险 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-15 | `RES-001`、`PLAN-001` | HEAD `7bbe4bc`；`internal/business -> internal/composition` 调用链；package graph；012/014 研究；`go test ./...` 通过；用户选择“模块”语义 | 未提交（计划门禁要求） | owner ID 是可观测诊断语义；历史文档需区分当前入口与当时快照，等待实施确认 |
| 2 | 2026-08-15 | `PATH-001`、`SEM-001`、`GOV-001`、`DOC-001`、`VER-001` | `origin/main...HEAD = 0/0`；旧目录不存在；`go list` 仅列出 `internal/module`；定向/full/race test、vet、build、tidy、27 个变更 Go 文件 gofmt、25 份 metadata、24 份 Markdown 链接、UTF-8/空白与 Diff 检查通过 | 本任务 Conventional Commit（见 Git 历史） | 全仓 `gofmt -l .` 会因未修改文件的 CRLF 和忽略的 `tmp/backend` 输出；未扩大范围格式化 |
