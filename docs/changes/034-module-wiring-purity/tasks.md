# 任务：业务模块装配纯度与文档一致性

## 1. 门禁状态

- 研究门禁：已通过，证据为 `R001`、`R002`。
- 当前计划状态：待确认。
- 当前授权：用户仅请求研究并提交修复方案；未授权实施。实施必须等待用户在计划报告后的后续消息中明确确认 034 当前方案。
- 实施前提：尚未满足；命中 design 第 8 节触发器后恢复待确认。
- 外部副作用：确认后的实施只修改仓库内源码、测试、生成器注册与文档目录；不 push、不 tag、不 release、不部署、不写外部数据库。

## 2. 任务清单

| ID | 工作量 | 依赖 | 任务 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `RES-001` | L | 无 | 审计模块装配流程：接入是否需要跨层多处修改、反向依赖、职责越界 | R001 区分事实、推断与偏移清单 | 已完成 |
| `RES-002` | L | `RES-001` | 核对权威文档与实现一致性 | R002 区分一致/需收口的偏移点 | 已完成 |
| `PLAN-001` | L | `RES-001..002` | 冻结 034 需求、设计、文件影响、风险与验证 | README、requirements、design、tasks 完整且相互引用 | 已完成（待确认） |
| `WIRE-001` | M | 用户确认 | 收敛 composition 的 HTTP 契约接入：泛化 `contractDispatcher`，`operationPolicies`/ops 从装配汇总而非硬编码 Todo | `<span class="math-inline">ops.go`/`service.go` 不再直接 import `todohttp.ModuleContract()` 复制契约；统一接收点建立 | 待确认 |
| `GEN-001` | M | `WIRE-001` | 处理 `registeredModules()`：方案 A 文档化注册点，或方案 B 复用装配汇总清单 | 注册职责明确；与文档一致 | 待确认 |
| `DOC-001` | M | `WIRE-001`、`GEN-001` | 同步模块开发指南（新增接入流程/注册点）、模块 README、api/README、配置说明、门禁说明 | 权威文档 == 当前能力；无漂移清单 | 待确认 |
| `GOV-001` | M | `WIRE-001` | 若可行，门禁补充反例（composition 反向 import 模块契约包）；否则以架构测试 + 文档约束 | 032/033 门禁继续通过；反向读取被约束或明确豁免 | 待确认 |
| `VER-001` | L | 全部实施任务 | 单元/集成/完整 gate + 文档一致性复核 | requirements 第 7 节全部有直接证据；无旧 Todo 涤具/反向读取残留 | 待确认 |

## 3. 实施顺序

```text
WIRE-001 -> GEN-001 -> DOC-001
  -> GOV-001 -> VER-001
```

先收敛 HTTP 契约接入与跨模块反向读取，再收口生成器注册与文档，最后门禁与验证。

## 4. 计划报告后的确认要求

1. 用户阅读研究结论、需求、设计与任务后，在后续消息中明确确认「034 当前方案」；
2. 确认后按任务 ID 顺序实施；范围外的新事实退回研究阶段；
3. 实施中的每个检查点提交只包含 034 范围文件；不 push、不 tag、不 release。

## 5. 停止条件

- 用户未确认当前计划；
- 命中 design 第 8 节重新确认触发器；
- 工作区出现无法可靠分离的用户修改；
- 测试、race、vet、build、tidy、生成或门禁失败且无法在确认范围内修复；
- 为继续工作必须保留 Todo 专属涤具、跨模块反向读取或双套装配。

停止时保留研究、计划与已确认任务范围，不以占位实现或降低门禁冒充完成。

## 6. 建议实施提交信息（确认后）

```text
refactor(composition): converge module HTTP wiring and align docs
```
