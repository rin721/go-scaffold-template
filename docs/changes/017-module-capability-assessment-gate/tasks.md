# 任务：应用模块能力评估门禁

## 1. 门禁状态

- 研究门禁：已通过，证据为 `R001`。
- 当前计划状态：已完成。
- 当前授权：用户在计划报告后的后续消息中明确确认“实施 017 当前方案”，授权当前 `AGT-001`、`GUIDE-001`、`NAV-001`、`KAPP-001`、`RSH-001`、`EX-001`、`DOC-001`、`VER-001`。
- 实施性质：纯文档/规则变更；没有修改 Go、配置、脚本、依赖或自动门禁实现。
- 提交约束：验证通过后只暂存并提交本任务文件。

## 2. 任务清单

| ID | 工作量 | 依赖 | 任务 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `RES-001` | M | 无 | 复核当前规则、导航、Capability 清单、Kernel App 形态、Todo 先例和架构门禁 | `R001` 区分事实、推断、局限与方案影响，研究门禁通过 | 已完成 |
| `PLAN-001` | M | `RES-001` | 冻结需求、文档 owner、导航、评估表、契约缺口升级和验证方案 | `requirements.md`、`design.md`、`tasks.md` 完整并引用 `R001` | 已完成 |
| `AGT-001` | S | `PLAN-001`、实施确认 | 在 `AGENTS.md` 增加通用触发语境与项目文档导航规则 | 不包含项目专属目录、API、策略枚举或产品清单；缺口时停止在研究/计划 | 已完成 |
| `GUIDE-001` | L | `AGT-001` | 新建应用模块开发当前权威指南 | 覆盖用例、能力清单、owner、配置、生命周期、契约适配、升级和验收 | 已完成 |
| `NAV-001` | S | `GUIDE-001` | 更新根 README、`docs/README.md` 和 `internal/module/README.md` 导航 | 从 Agent 规则到项目指南的单轨路径可达，无正文复制 | 已完成 |
| `KAPP-001` | S | `GUIDE-001` | 更新 Kernel App 指南的准入与反向导航 | 只有先判定为进程选择的底层能力才进入组件形态选择；未支持语义必须升级 | 已完成 |
| `RSH-001` | S | `GUIDE-001` | 更新研究档案规范 | 新模块/外部系统研究必须显式提交能力差距、资源 owner、生命周期和契约适配结论 | 已完成 |
| `EX-001` | M | `GUIDE-001` | 在指南补充消息、邮件和搜索例子 | 同时覆盖底层资源与模块业务语义，不预选供应商或声称已实现 | 已完成 |
| `DOC-001` | S | `AGT-001`、`GUIDE-001`、`NAV-001`、`KAPP-001`、`RSH-001`、`EX-001` | 同步 017 状态和变更索引 | 当前权威与历史记录职责一致，任务证据完整 | 已完成 |
| `VER-001` | M | 全部实施任务 | 验证链接、术语、单一权威、状态和 Diff | 全部文档链接有效；定向搜索符合设计；`git diff --check` 通过 | 已完成 |

## 3. 实施顺序

```text
AGT-001
  -> GUIDE-001
     -> NAV-001 + KAPP-001 + RSH-001 + EX-001
        -> DOC-001
           -> VER-001
```

`AGT-001` 先冻结通用边界；`GUIDE-001` 再承接项目特定细节。这样不会把尚未落到项目文档的空导航提前写进规则，也不会把具体推进方式反向堆进 `AGENTS.md`。

## 4. 实施与验证证据

| 轮次 | 日期 | 完成任务 | 证据 | Commit | 剩余风险 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-15 | `RES-001`、`PLAN-001` | HEAD `2a0b454`；当前规则、导航、Capabilities、Kernel App、Todo 先例与架构测试静态复核 | 未提交（计划门禁要求） | 未进行多模型行为实验 |
| 2 | 2026-08-15 | `AGT-001`、`GUIDE-001`、`NAV-001`、`KAPP-001`、`RSH-001`、`EX-001`、`DOC-001`、`VER-001` | Markdown 相对链接、尾随空白、必答章节、通用 AGENTS 边界、Kernel 形态一致性、未实现能力声明和 `git diff --check` 全部通过 | `b113f90` | 文档门禁不能机械判断业务结论正确性 |

验证范围：

- `AGENTS.md -> README.md -> docs/README.md -> 应用模块开发指南` 导航闭合。
- 所有本任务 Markdown 相对链接均指向现有文件；围栏代码和行内代码不作为链接解析。
- `AGENTS.md` 新规则不包含项目目录、组件 API、Reload 枚举或具体产品。
- 指南包含能力评估表、资源/运行 owner、当前契约不适用、消息、邮件、搜索和研究输出章节。
- 指南中的 `Value`、`ManagedFixed`、`ManagedConfigured`、Replacement、`NoReload`、`KernelInstanceSwap` 与 `RestartRequired` 与 Kernel App 当前说明一致。
- 文档明确消息、邮件、搜索仍未选型和未实现通用 Capability。
- 所有本任务文档无尾随空白，`git diff --check` 通过。

本轮没有修改 Go 文件，因此未运行 `go test ./...`，也不以 Go 测试证明文档流程语义。

## 5. 剩余风险

- 文档门禁能强制交付栏目，不能机械证明 Agent 的业务判断正确；仍需代码事实和评审。
- 具体新能力可能同时包含共享底层资源和模块专属长期任务，不能用单一 owner 粗暴归类。
- 当前 Kernel 不支持的语义会随实现演进；指南必须链接当前 Kernel App 文档，而不是复制容易漂移的完整内部契约。
