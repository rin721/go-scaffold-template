# 任务：脚手架产品形态与升级模型

## 1. 门禁状态

- 020 研究门禁：已通过。
- 计划状态：待确认。
- 本轮授权：只完成研究与计划文档并提交。
- 非文档验证：必须在本计划报告后的后续用户消息中明确确认下列 `PROBE-*` 与 `ADR-001`，当前不得执行。

## 2. 任务清单

| ID | 工作量 | 依赖 | 内容 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `RES-001` | M | `019/FORM-001` | 核验当前 library/template/generator/upgrade 事实 | R001 可复核且区分事实与推断 | 已完成 |
| `RES-002` | M | 无 | 研究 Go module、`internal`、模板和版本官方语义 | R002 只使用主源并说明适用边界 | 已完成 |
| `PLAN-001` | L | `RES-001`、`RES-002` | 建立四形态比较、需求、隔离验证和决策规则 | requirements/design/tasks 完整 | 已完成 |
| `PROBE-001` | M | 用户确认 | 在忽略目录验证身份化创建、Todo/none、原子输出、所有权 manifest 与确定性 | 产物编译/测试；身份扫描和重复生成检查有证据 | 待确认 |
| `PROBE-002` | M | 用户确认 | 验证当前 external/internal 负向边界，并在快照中度量最小公共 Runtime seam | 不修改生产源码；列出最小 API、owner 和泄漏风险 | 待确认 |
| `PROBE-003` | L | `PROBE-001`、`PROBE-002` | 模拟 Runtime 与 template schema 分轨升级、用户 sentinel 和冲突停止 | 两类升级证据完整，用户文件未被静默覆盖 | 待确认 |
| `ADR-001` | M | 三个 `PROBE` | 根据硬门禁决定唯一产品形态及版本/所有权模型 | ADR、020 文档和 019 状态同步；不夹带正式实现 | 待确认 |

## 3. 确认后的允许范围

- 只允许在 Git 忽略的 `tmp/scaffold-product-form/` 创建可丢弃消费者、source snapshot、原型脚本和证据。
- 允许运行静态检查、生成、`go test`、`go build` 和预期失败的跨 module 导入验证。
- 允许新增/更新 020 文档和一份正式 ADR；若 ADR 导航需要同步，可修改相应文档索引。
- 不允许修改 `cmd/`、`internal/`、`pkg/`、根配置、依赖声明、CI 或发布状态；这些由 ADR 后的新任务承接。
- 不允许 tag、push、启动长期进程或写入外部系统。

## 4. 逐轮证据

| 轮次 | 日期 | 完成任务 | 证据 | Commit | 剩余风险 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-08-15 | `RES-001`、`RES-002`、`PLAN-001` | HEAD `af7fdad`；仓库身份/边界/tag 清单；`go help gopath`；Go/GitHub 官方资料；文档验证 | 本次纯文档提交 | 尚无外部消费者、生成、升级或公共 Runtime 实验证据 |

## 5. 当前停止点

请在计划报告后明确确认 `PROBE-001`、`PROBE-002`、`PROBE-003` 与 `ADR-001`，Agent 才能进入隔离验证。此前的“执行 019”只授权启动 020 的研究与计划，不能跨越新计划的确认门禁。
